└ program
  └ statement
    └ while_stm
      └ WHILE
      └ LPAREN
      └ condition
        └ expression
          └ term
            └ factor
              └ ID
        └ comparison_op
          └ EQ
        └ expression
          └ term
            └ factor
              └ NUM_INT
      └ RPAREN
      └ LBRACE
      └ statement
        └ assign_stm
          └ ID
          └ ASSIGN
          └ right_value
            └ expression
              └ term
                └ factor
                  └ NUM_INT
          └ SEMI
      └ RBRACE
  └ statement
    └ if_stm
      └ IF
      └ LPAREN
      └ condition
        └ expression
          └ term
            └ factor
              └ ID
        └ comparison_op
          └ NEQ
        └ expression
          └ term
            └ factor
              └ NUM_INT
      └ RPAREN
      └ LBRACE
      └ statement
        └ assign_stm
          └ ID
          └ ASSIGN
          └ right_value
            └ expression
              └ term
                └ factor
                  └ NUM_INT
          └ SEMI
      └ RBRACE
  └ statement
    └ for_stm
      └ FOR
      └ LPAREN
      └ declare_stm
        └ INT
        └ ID
        └ ASSIGN
        └ right_value
          └ expression
            └ term
              └ factor
                └ NUM_INT
        └ SEMI
      └ condition
        └ expression
          └ term
            └ factor
              └ ID
        └ comparison_op
          └ LT
        └ expression
          └ term
            └ factor
              └ NUM_INT
      └ SEMI
      └ ID
      └ ASSIGN
      └ right_value
        └ expression
          └ term
            └ factor
              └ ID
          └ ADD
          └ term
            └ factor
              └ NUM_INT
      └ RPAREN
      └ statement
        └ assign_stm
          └ ID
          └ ASSIGN
          └ right_value
            └ expression
              └ term
                └ factor
                  └ ID
          └ SEMI

└ program
  └ statement
    └ if_stm
      └ IF
      └ LPAREN
      └ condition
        └ expression
          └ term
            └ factor
              └ ID
        └ comparison_op
          └ NEQ
        └ expression
          └ term
            └ factor
              └ ID
      └ RPAREN
      └ LBRACE
      └ statement
        └ assign_stm
          └ ID
          └ ASSIGN
          └ right_value
            └ expression
              └ term
                └ factor
                  └ ID
          └ SEMI
      └ RBRACE
      └ ELSE
      └ statement
        └ if_stm
          └ IF
          └ LPAREN
          └ condition
            └ expression
              └ term
                └ factor
                  └ ID
            └ comparison_op
              └ EQ
            └ expression
              └ term
                └ factor
                  └ ID
          └ RPAREN
          └ LBRACE
          └ statement
            └ assign_stm
              └ ID
              └ ASSIGN
              └ right_value
                └ expression
                  └ term
                    └ factor
                      └ ID
                  └ ADD
                  └ term
                    └ factor
                      └ NUM_INT
              └ SEMI
          └ RBRACE
          └ ELSE
          └ LBRACE
          └ statement
            └ assign_stm
              └ ID
              └ ASSIGN
              └ right_value
                └ expression
                  └ term
                    └ factor
                      └ ID
                  └ ADD
                  └ term
                    └ factor
                      └ NUM_INT
              └ SEMI
          └ RBRACE
  └ statement
    └ while_stm
      └ WHILE
      └ LPAREN
      └ condition
        └ expression
          └ term
            └ factor
              └ ID
        └ comparison_op
          └ EQ
        └ expression
          └ term
            └ factor
              └ ID
      └ RPAREN
      └ LBRACE
      └ statement
        └ while_stm
          └ WHILE
          └ LPAREN
          └ condition
            └ expression
              └ term
                └ factor
                  └ ID
            └ comparison_op
              └ EQ
            └ expression
              └ term
                └ factor
                  └ ID
          └ RPAREN
          └ LBRACE
          └ statement
            └ assign_stm
              └ ID
              └ ASSIGN
              └ right_value
                └ expression
                  └ term
                    └ factor
                      └ ID
              └ SEMI
          └ RBRACE
      └ statement
        └ assign_stm
          └ ID
          └ ASSIGN
          └ right_value
            └ expression
              └ term
                └ factor
                  └ ID
          └ SEMI
      └ statement
        └ while_stm
          └ WHILE
          └ LPAREN
          └ condition
            └ expression
              └ term
                └ factor
                  └ ID
            └ comparison_op
              └ EQ
            └ expression
              └ term
                └ factor
                  └ ID
          └ RPAREN
          └ LBRACE
          └ statement
            └ assign_stm
              └ ID
              └ ASSIGN
              └ right_value
                └ expression
                  └ term
                    └ factor
                      └ ID
              └ SEMI
          └ RBRACE
      └ RBRACE

└ program
  └ statement
    └ declare_stm
      └ INT
      └ ID
      └ array
        └ LBRACKET
        └ NUM_INT
        └ RBRACKET
      └ SEMI
  └ statement
    └ assign_stm
      └ ID
      └ ASSIGN
      └ right_value
        └ expression
          └ term
            └ factor
              └ ID
              └ array
                └ LBRACKET
                └ NUM_INT
                └ RBRACKET
      └ SEMI
  └ statement
    └ assign_stm
      └ ID
      └ array
        └ LBRACKET
        └ NUM_INT
        └ RBRACKET
      └ ASSIGN
      └ right_value
        └ expression
          └ term
            └ factor
              └ ID
              └ array
                └ LBRACKET
                └ NUM_INT
                └ RBRACKET
      └ SEMI
